<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.musicplayersam">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".AppContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.main.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.main.AppMainActivity" />

        <activity
            android:name=".ui.activity.MusicPlayerActivity"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".ui.activity.cloud.CloudPlayListDetailActivity"
             />

        <activity android:name=".ui.activity.cloud.CloudDailyRecommendActivity" />

        <activity android:name=".ui.activity.cloud.TopDetailActivity" />

        <activity android:name=".ui.activity.user.LoginActivity" />

        <activity android:name=".ui.activity.local.LocalMusicActivity"
            android:theme="@style/AppTheme.ColoredNavigationBar"/>

        <activity android:name=".ui.activity.local.LocalMusicListActivity" />

        <activity android:name=".ui.activity.local.LocalScannerActivity"
            android:theme="@style/AppTheme.ColoredNavigationBar"/>

    </application>

</manifest>